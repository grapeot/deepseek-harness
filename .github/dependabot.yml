# Personal base fork: dependency updates are taken by rebasing on upstream
# (deepseek-harness/deepseek-harness), not by per-fork bumps. Disable all
# Dependabot version updates; security alerts stay off at the repo level.
version: 2
updates: []
